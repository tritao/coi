component App {
    mut int count = 0;

    inc {
        count += 1;
    }

    view {
        <div class="root fill pad-0 gap-0">
            <div class="btn w-100 h-30 pad-0" onclick={inc}>Count:{count}</div>
        </div>
    }
}

app {
    root = App;
}

